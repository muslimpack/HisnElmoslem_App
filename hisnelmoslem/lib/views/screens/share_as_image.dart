import 'package:flutter/widgets.dart';

class ShareAsImage extends StatelessWidget {
  const ShareAsImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
